{
  "version": 1,
  "comment": "skill-forge rules table. Add a file type = add a block to types; package_checks are builtin checks that ignore suffix and cover the whole package. Paths given on the command line are appended to each command by the engine.",
  "tools": {
    "ruff": { "suffixes": [".py"] },
    "ty": { "suffixes": [".py"] },
    "rumdl": { "suffixes": [".md"] },
    "oxlint": {
      "suffixes": [".js", ".jsx", ".ts", ".tsx", ".mjs", ".cjs", ".mts", ".cts"],
      "no_target_markers": ["No files found to lint"]
    },
    "oxfmt": {
      "suffixes": [
        ".js",
        ".jsx",
        ".ts",
        ".tsx",
        ".mjs",
        ".cjs",
        ".mts",
        ".cts",
        ".css",
        ".scss",
        ".less",
        ".json"
      ],
      "no_target_markers": ["Expected at least one target file"]
    },
    "oxipng": { "suffixes": [".png"] }
  },
  "package_checks": [{ "check": "no-local-paths" }],
  "types": [
    {
      "id": "python",
      "label": "Python",
      "suffixes": [".py"],
      "builtins": [{ "check": "python-syntax" }],
      "fix": [
        ["ruff", "check", "--no-cache", "--fix", "--unsafe-fixes"],
        ["ruff", "format", "--no-cache"],
        ["ty", "check", "--fix"]
      ],
      "verify": [
        ["ruff", "check", "--no-cache"],
        ["ruff", "format", "--no-cache", "--check"],
        ["ty", "check", "--output-format", "concise"]
      ]
    },
    {
      "id": "markdown",
      "label": "Markdown",
      "suffixes": [".md"],
      "builtins": [{ "check": "skill-frontmatter", "only": ["SKILL.md"] }],
      "fix": [
        ["rumdl", "check", "--no-cache", "--fix"],
        ["rumdl", "fmt", "--no-cache"]
      ],
      "verify": [
        ["rumdl", "check", "--no-cache"],
        ["rumdl", "fmt", "--no-cache", "--check"]
      ]
    },
    {
      "id": "json-data",
      "label": "JSON",
      "suffixes": [".json", ".jsonc"],
      "builtins": [{ "check": "json-parse" }],
      "fix": [],
      "verify": []
    },
    {
      "id": "frontend",
      "label": "TS/JS",
      "suffixes": [".js", ".jsx", ".ts", ".tsx", ".mjs", ".cjs", ".mts", ".cts"],
      "builtins": [],
      "fix": [
        ["oxlint", "--fix", "--format", "unix"],
        ["oxfmt"]
      ],
      "verify": [
        ["oxlint", "--deny-warnings", "--format", "unix"],
        ["oxfmt", "--check"]
      ]
    },
    {
      "id": "css",
      "label": "CSS",
      "suffixes": [".css", ".scss", ".less"],
      "builtins": [],
      "fix": [["oxfmt"]],
      "verify": [["oxfmt", "--check"]]
    },
    {
      "id": "png",
      "label": "PNG",
      "suffixes": [".png"],
      "builtins": [{ "check": "png-integrity" }],
      "fix": [["oxipng", "-o", "2"]],
      "verify": [],
      "report_savings": true
    }
  ]
}
